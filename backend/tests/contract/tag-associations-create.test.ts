import { describe, it } from 'vitest';

// Legacy tag association create contract replaced by the consolidated tags suite.
describe.skip('Legacy /tags/:id/associations create contract (removed)', () => {
  it('placeholder', () => {
    // no-op
  });
});