import { describe, it } from 'vitest';

// Legacy tag associations delete contract replaced by the consolidated tags suite.
describe.skip('Legacy /tags/:id/associations delete contract (removed)', () => {
  it('placeholder', () => {
    // no-op
  });
});