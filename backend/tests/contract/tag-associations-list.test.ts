import { describe, it } from 'vitest';

// Legacy tag associations list contract replaced by the consolidated tags suite.
describe.skip('Legacy /tags/:id/associations list contract (removed)', () => {
  it('placeholder', () => {
    // no-op
  });
});