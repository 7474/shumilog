import { describe, it } from 'vitest';

// Legacy tags create contract replaced by the consolidated tags suite.
describe.skip('Legacy /tags create contract (removed)', () => {
  it('placeholder', () => {
    // no-op
  });
});