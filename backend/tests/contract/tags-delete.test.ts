import { describe, it } from 'vitest';

// Legacy tags delete contract replaced by the consolidated tags suite.
describe.skip('Legacy /tags/:id delete contract (removed)', () => {
  it('placeholder', () => {
    // no-op
  });
});