import { describe, it } from 'vitest';

// Legacy tags detail contract replaced by the consolidated tags suite.
describe.skip('Legacy /tags/:id detail contract (removed)', () => {
  it('placeholder', () => {
    // no-op
  });
});