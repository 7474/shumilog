import { describe, it } from 'vitest';

// Legacy tags list contract replaced by the consolidated tags suite.
describe.skip('Legacy /tags list contract (removed)', () => {
  it('placeholder', () => {
    // no-op
  });
});