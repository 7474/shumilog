import { describe, it } from 'vitest';

// Legacy tags update contract replaced by the consolidated tags suite.
describe.skip('Legacy /tags/:id update contract (removed)', () => {
  it('placeholder', () => {
    // no-op
  });
});