import { describe, it } from 'vitest';

// Legacy contract tests replaced by `users.contract.test.ts`.
// Marked as skipped placeholder so Vitest discovers the file without executing tests.
describe.skip('Legacy users/me contract (removed)', () => {
	it('placeholder', () => {
		// no-op
	});
});